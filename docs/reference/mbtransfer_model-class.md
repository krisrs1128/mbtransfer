# Transfer Function Model Class

Transfer Function Model Class

## Slots

- `parameters`:

  The model training result. For mbtransfer models, this is a list of
  all the boosting models that have been trained, one for each taxon.

- `method`:

  The model type used for prediction.

- `hyper`:

  A list. containing any hyperparameters used during model training.
