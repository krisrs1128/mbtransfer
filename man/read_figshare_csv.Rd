% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{read_figshare_csv}
\alias{read_figshare_csv}
\title{Read a CSV file hosted on Figshare}
\usage{
read_figshare_csv(url, ...)
}
\arguments{
\item{url}{A Figshare \code{ndownloader} URL in either of the two common forms:
\itemize{
\item \code{"https://ndownloader.figshare.com/files/40275934"} (canonical)
\item \code{"https://figshare.com/ndownloader/files/40275934/subject.csv"}
(also accepted; the hostname and optional trailing filename are
normalised automatically).
}}

\item{...}{Additional arguments forwarded to \code{\link[readr:read_csv]{readr::read_csv()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} as returned by \code{\link[readr:read_csv]{readr::read_csv()}}.
}
\description{
Figshare's download redirector (\code{ndownloader}) can block a bare
\code{read_csv(url)} call because the \code{figshare.com/ndownloader} redirect path
returns a 202 Accepted response instead of a direct 302 → S3 hop.
This wrapper normalises both common URL forms to the
\code{ndownloader.figshare.com} subdomain (which redirects reliably to S3),
downloads the file with \code{\link[=download.file]{download.file()}}, then hands the local copy to
\code{\link[readr:read_csv]{readr::read_csv()}}.  Within an R session the file is kept in a
temporary cache directory so repeated calls for the same URL skip the
network entirely.
}
